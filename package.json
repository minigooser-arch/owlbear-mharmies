{
  "name": "letopis-armies",
  "private": true,
  "version": "1.2.4",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -b && vite build && node scripts/preserve-legacy-assets.mjs && node scripts/stamp-manifest.mjs && npm run verify:dist && npm run smoke:dist",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run lint && npm test && npm run build && npm run probe:live",
    "verify:dist": "node scripts/verify-dist.mjs",
    "smoke:dist": "node scripts/smoke-built-popover.mjs",
    "probe:live": "node scripts/probe-live-pages.mjs"
  },
  "dependencies": {
    "@owlbear-rodeo/sdk": "3.1.0",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@testing-library/jest-dom": "6.9.1",
    "@testing-library/react": "16.3.2",
    "@types/node": "24.10.1",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.3",
    "eslint": "10.7.0",
    "jsdom": "29.1.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.63.0",
    "vite": "8.1.4",
    "vitest": "4.1.11"
  }
}
