name: Apply naval area validation fix

on:
  push:
    branches: [feature/naval-combat-v1]
    paths:
      - scripts/apply-naval-area-validation-fix.py
      - .github/workflows/apply-naval-area-validation-fix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/naval-combat-v1
      - name: Apply patch
        run: python3 scripts/apply-naval-area-validation-fix.py
      - name: Remove one-shot files
        run: rm scripts/apply-naval-area-validation-fix.py .github/workflows/apply-naval-area-validation-fix.yml
      - name: Commit production fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: reject non-sea naval battle areas"
          git push origin HEAD:feature/naval-combat-v1
