name: Apply shared army ship detection

on:
  push:
    branches:
      - feature/naval-combat-v1
    paths:
      - .github/workflows/shared-unit-detection-green.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/naval-combat-v1
          fetch-depth: 0
      - run: python scripts/apply-shared-unit-detection.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/background/application.ts
          git rm scripts/apply-shared-unit-detection.py
          git rm .github/workflows/shared-unit-detection-green.yml
          git commit -m "feat: share detection between armies and ships"
          git push origin HEAD:feature/naval-combat-v1
