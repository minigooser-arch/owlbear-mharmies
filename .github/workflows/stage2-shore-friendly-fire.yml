name: Stage2 Shore Friendly Fire Patch

on:
  push:
    branches: [feature/naval-stage2-runtime]
    paths:
      - scripts/stage2-shore-friendly-fire.mjs
      - .github/workflows/stage2-shore-friendly-fire.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/naval-stage2-runtime
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: node scripts/stage2-shore-friendly-fire.mjs
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/shared/types.ts src/commands/commandValidation.ts src/naval/shore/shoreBombardment.ts src/commands/commandProcessor.ts src/commands/navalShoreBombardmentCommands.test.ts src/commands/navalShoreBombardmentWindow.test.ts
          git diff --cached --quiet && exit 1
          git commit -m "feat: require shore friendly fire confirmation"
          git push origin HEAD:feature/naval-stage2-runtime
