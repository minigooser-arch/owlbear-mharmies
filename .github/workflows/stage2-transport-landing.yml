name: Stage2 Transport Landing Patch

on:
  push:
    branches: [feature/naval-stage2-runtime]
    paths:
      - scripts/stage2-transport-landing.mjs
      - .github/workflows/stage2-transport-landing.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: feature/naval-stage2-runtime
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: node scripts/stage2-transport-landing.mjs
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/shared/constants.ts src/ui/state/useExtensionState.ts src/ui/pages/FleetPage.tsx src/owlbear/extensionServices.ts src/background/application.ts
          git diff --cached --quiet && exit 1
          git commit -m "feat: wire transport landing map flow"
          git push origin HEAD:feature/naval-stage2-runtime
