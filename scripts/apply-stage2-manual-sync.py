from pathlib import Path

path = Path("docs/manual-four-client-test.md")
text = path.read_text(encoding="utf-8")
heading = "## Морской бой — Stage 2: береговой обстрел"
start = text.find(heading)
if start < 0:
    raise SystemExit("shore bombardment manual heading not found")
next_heading = text.find("\n## ", start + len(heading))
end = len(text) if next_heading < 0 else next_heading + 1

replacement = '''## Морской бой — Stage 2: береговой обстрел

1. Завершите или не запускайте морской бой и переведите глобальный ход в `POST_MOVEMENT`. Подготовьте живые `BATTLESHIP` и `CRUISER` стороны A и несколько армий: видимую вражескую B, скрытую вражескую B, собственную A и союзную C. Разместите положительную цель на `LAND` или mixed `LAND+SEA` внутри обычного broadside sector/range корабля и без препятствия naval LOS.

Ожидание: у ведущего и лидера A в карточках линкора/крейсера появляется выбор **Цель берегового обстрела**. В списке есть только живые армии, уже видимые этому клиенту; скрытая вражеская армия не появляется и её имя не просачивается в snapshot. `IRONCLAD`, `HOSPITAL` и `TRANSPORT` такого управления не получают.

2. Лидером A выберите видимую вражескую армию B и выполните обстрел линкором.

Ожидание: отправляется authoritative `NAVAL_SHORE_BOMBARDMENT`; линкор наносит `3d6` прямого урона армии. Броня кораблей к урону армии не относится. Стреляющий корабль раскрывается стороне цели через следующий глобальный ход. Если HP армии достигает 0, она уничтожается обычным army lifecycle.

3. На другом доступном ходу повторите положительный сценарий крейсером.

Ожидание: крейсер наносит `2d6`. После успешного обстрела этот же корабль больше не получает второй береговой выстрел в том же глобальном ходу.

4. По отдельности поставьте видимую цель вне broadside sector, вне range, на клетку без `LAND` и за препятствие naval LOS. При необходимости цель всё ещё может оставаться в минимальном UI-списке — интерфейс специально не дублирует геометрию боевого движка.

Ожидание: authoritative команда отвергает каждую неверную цель соответствующей причиной; урон, reveal и отметка использованного выстрела не применяются. UI не является источником истины для sector/range/terrain/LOS.

5. Выберите собственную армию A или армию союзной стороны C.

Ожидание: перед отправкой команды появляется явное подтверждение friendly fire. Отмена не отправляет обстрел. После подтверждения команда уходит с `friendlyFireConfirmed: true`. Для вражеской и нейтральной цели такого подтверждения нет.

6. Верните фазу в `MOVEMENT`, а затем отдельно создайте активный морской бой и проверьте те же корабли.

Ожидание: role-safe список целей берегового обстрела пуст и UI-кнопка отсутствует. Даже stale/ручная команда в `MOVEMENT` отвергается как `NOT_POST_MOVEMENT_PHASE`, а при активном морском бою — как `NAVAL_BATTLE_ACTIVE`.

## Морской бой — Stage 2: крейсерский перехват

1. Запустите морской бой с живым `CRUISER` стороны A и минимум одним кораблём B. Дождитесь активного хода крейсера A. Сначала активируйте **Перехват** из карточки корабля.

Ожидание: крейсер тратит обычное naval action, его оставшиеся тактические ОП становятся 0, его ход немедленно завершается и инициатива переходит следующему кораблю. Создаётся одна активная interception zone.

2. Сравните четыре клиента сразу после активации.

Ожидание: точная зона перехвата показывается локальным overlay только ведущему и лидеру стороны A. Обычный участник A и противник B её не видят. Зона строится по текущей позиции и курсу крейсера, использует его точную normal broadside mask и текущий naval LOS; это не публичный объект сцены.

3. Для отдельного запуска закройте панель расширения до хода крейсера, нажмите ПКМ по его токену и выберите **Перехват**.

Ожидание: действие доступно из постоянного background runtime и срабатывает без открытого popover. Оно отправляет тот же authoritative `NAVAL_ACTIVATE_INTERCEPTION`, что и кнопка карточки, с теми же правилами авторизации и расхода хода.

4. После активации поставьте корабль B снаружи зоны так, чтобы его следующий разрешённый шаг вошёл в клетку зоны, и выполните движение.

Ожидание: корабль B остаётся в клетке, в которую вошёл; перехват наносит `2d6` с обычным вычитанием брони цели. Зона расходуется, оставшиеся ОП цели становятся 0, её action считается использованным, её текущий ход немедленно завершается и инициатива переходит дальше.

5. Повторите вход с целью, чья броня полностью поглощает бросок.

Ожидание: даже при 0 фактического урона сработавшая зона всё равно расходуется и активация вошедшего корабля завершается.

6. Проверьте отрицательные варианты: корабль той же стороны проходит через сектор; чужой корабль начинает шаг уже внутри зоны и остаётся внутри; чужой корабль двигается снаружи, но не входит в зону.

Ожидание: перехват не срабатывает и зона остаётся активной.

7. Не активируя зону, последовательно завершайте ходы остальных кораблей раунда.

Ожидание: зона сохраняется между чужими ходами и исчезает ровно тогда, когда этот крейсер снова становится активным в следующем раунде.

8. В отдельном сценарии нанесите крейсеру с ещё не использованной зоной положительную фактическую потерю HP обычным бортовым залпом.

Ожидание: interception zone немедленно удаляется. Если броня/temporary HP приводят к отсутствию положительной фактической потери HP по правилу удаления, зона не должна исчезать только из-за факта атаки.

9. Завершите морской бой при активной неиспользованной зоне.

Ожидание: состояние перехвата и приватный local overlay удаляются вместе с завершением боя; после боя остаточной зоны ни на одном клиенте нет.
'''

text = text[:start] + replacement + ("\n" + text[end:] if end < len(text) else "")
path.write_text(text, encoding="utf-8")
